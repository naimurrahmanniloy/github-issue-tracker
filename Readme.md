Github-Issue-Tracker

1. What is the difference between var, let, and const?

Ans: There is a lot of difference between var, let, and const. So main difference is global scope. Var value can access everywhere in the code.But the const and let are block scope the value of let is reassignable and the value of const is not assignable after it declared.

2. What is the spread operator (...)?

Ans: Spread operator is a ES6 Js method. It is used for copy the existing array or object data into another array or object

3. What is the difference between map(), filter(), and forEach()?

Ans: The difference between map(), filter(), and forEach():

- map() is return a new array with element .
- the forEach() return nothing .
- filter() used for getting the data math with condition from array.

4. What is an arrow function?

Ans: An arrow function is a shorter way to write a function in JavaScript. It is popular in modern ES6 code because it keeps your logic compact and often makes callbacks easier to read.

5. What are template literals?

Ans: Template literals are also known as template strings. They are a feature in JavaScript that allow for easier string interpolation and multi-line strings.
